# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: yt-notes-backend
metadata:
  template: yt-notes-backend@0.0.1-beta

services:
  api:
    project: ./backend
    language: python
    host: appservice
