# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (.env.local)
# =============================================================================
# Copy this file to `.env.local` and fill in your actual values
# NEVER commit the actual `.env.local` file to version control

# -----------------------------------------------------------------------------
# Backend API URL (ONLY variable needed!)
# -----------------------------------------------------------------------------
# Local development
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Production (Railway backend)
# NEXT_PUBLIC_BACKEND_URL=https://your-backend.railway.app

# -----------------------------------------------------------------------------
# ✅ NO SUPABASE CREDENTIALS NEEDED!
# -----------------------------------------------------------------------------
# All authentication is handled through the backend API
# The frontend does NOT need:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
#
# The backend handles all Supabase interactions securely

# -----------------------------------------------------------------------------
# SECURITY BENEFITS
# -----------------------------------------------------------------------------
# ✅ No Supabase credentials exposed to browser
# ✅ All database access goes through backend
# ✅ Authentication handled server-side
# ✅ JWT tokens stored securely in localStorage
# ✅ Automatic token refresh
# ✅ RLS protection + backend-only access
