# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (.env)
# =============================================================================
# Copy this file to `.env` and fill in your actual values
# NEVER commit the actual `.env` file to version control

# -----------------------------------------------------------------------------
# YouTube API
# -----------------------------------------------------------------------------
# Get your API key from: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your_youtube_api_key_here

# -----------------------------------------------------------------------------
# Supabase Database 1 (Videos/Notes)
# -----------------------------------------------------------------------------
# Get from: https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/api
SUPABASE_URL=your_supabase_url
# Use SERVICE_ROLE key for backend (bypasses RLS)
SUPABASE_KEY=your_supabase_service_role_key
SUPABASE_SERVICE_KEY=your_supabase_service_role_key
# JWT secret for token verification (Settings > API > JWT Settings)
SUPABASE_JWT_SECRET=your_jwt_secret

# -----------------------------------------------------------------------------
# Supabase Database 2 (Books)
# -----------------------------------------------------------------------------
SUPABASE_URL_2=your_supabase_url_2
SUPABASE_KEY_2=your_supabase_service_role_key_2
SUPABASE_SERVICE_KEY_2=your_supabase_service_role_key_2

# -----------------------------------------------------------------------------
# Database (Direct PostgreSQL connection - optional)
# -----------------------------------------------------------------------------
# Get from Supabase Settings > Database > Connection string
# Both databases share the same password
DB_PASSWORD=your_db_password

# -----------------------------------------------------------------------------
# OpenAI API
# -----------------------------------------------------------------------------
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Host and port for the API server
PORT=8000

# These are not used by railway, but kept for local dev
API_HOST=0.0.0.0
API_RELOAD=true

# -----------------------------------------------------------------------------
# CORS Origins (comma-separated)
# -----------------------------------------------------------------------------
# For local development
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
# For production (Railway + Vercel), add your deployed URLs:
# CORS_ORIGINS=https://your-app.vercel.app,https://your-backend.railway.app

# -----------------------------------------------------------------------------
# IMPORTANT: Frontend should NOT have these variables!
# -----------------------------------------------------------------------------
# The frontend should only have:
# - NEXT_PUBLIC_BACKEND_URL (pointing to this backend)
# - NEXT_PUBLIC_SUPABASE_URL (for auth only)
# - NEXT_PUBLIC_SUPABASE_ANON_KEY (for auth only)
#
# NEVER expose SERVICE_ROLE keys or JWT_SECRET to the frontend!
